import * as React from 'react';
import CircularProgress from '@mui/material/CircularProgress';

export default function CircularDeterminate() {
 

  return (
   
      
      <CircularProgress size={20} variant="determinate" value={75} />
 
  );
}
